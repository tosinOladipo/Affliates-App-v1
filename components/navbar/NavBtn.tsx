import React from 'react'
import { Button } from '../ui/button'
import Link from 'next/link'

function NavBtn({name, url} : {name : String, url : String}) {
  return (
   <p>NavBtn</p>
  )
}

export default NavBtn